# ─────────────────────────────────────────────────────────────────────────────
# Copie en « .env » au premier `npm run dev` si .env absent (.env n’est pas versionné).
#
# PAT GitHub (lecture repos publics). En CI : secret du dépôt API_KEY_PORTFOLIO
# (voir .github/workflows/ci.yml).
# ─────────────────────────────────────────────────────────────────────────────

API_KEY_PORTFOLIO=

GITHUB_TOKEN=

GITHUB_USERNAME=SandersonnDev
